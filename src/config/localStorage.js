const localStorage = {
    token: 'token',
    theme: 'data-theme',
    isConstrast: 'is-contrast',
    primaryColor: 'primary-color',
    layout: 'layout',
    idUser: 'id',
};

export default localStorage;
